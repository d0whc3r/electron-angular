Templates to run electron app using angular js
Included multi window electron with/without loading bars in main window when child is launched

Based on:
- yeoman electron generator
- yeoman angular generator

Commands:
grunt serve
npm run build-all
